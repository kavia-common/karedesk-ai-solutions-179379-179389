# Frontend environment variables for Karedesk frontend (React)
# Copy this file to `.env` in the same directory and adjust values as needed.

# Base URL for the backend API. Do not include a trailing slash.
REACT_APP_API_BASE_URL=http://localhost:3001
